import React from 'react'
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Toaster } from 'react-hot-toast'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'OpenScript - AI-Powered Viral Video Script Generator',
  description: 'Generate viral TikTok, Reels, and Shorts scripts using AI. Discover trending content, transcribe videos, and create engaging scripts with OpenScript.',
  keywords: 'TikTok, viral videos, script generator, AI, content creation, social media',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Toaster 
          position="top-right"
          toastOptions={{
            duration: 4000,
            style: {
              background: '#363636',
              color: '#fff',
            },
          }}
        />
      </body>
    </html>
  )
} 